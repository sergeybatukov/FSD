extends ../layout/main
block content
    .cards
        +number_search('cards')
        +registration('cards')
        +sing_in('cards')
        +hotel_number('cards')
        +number_card('cards', 1)
        +number_card('cards', 0)